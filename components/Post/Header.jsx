// import DateFormatter from '../components/DateFormatter'
import PostTitle from './Title'

const Header = ({ title, coverImage, author }) => {
  return <PostTitle>{title}</PostTitle>
}

export default Header
